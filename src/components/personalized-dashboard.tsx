'use client'\n\nimport { useNeuroFog } from './neurofog-provider'\nimport { getPersonalizationProfile } from '@/lib/personalization'\nimport { Card } from './ui/card'\nimport { Button } from './ui/button'\nimport { Badge } from './ui/badge'\nimport { Progress } from './ui/progress'\n\nexport function PersonalizedDashboard() {\n  const { \n    userType, \n    personalizedCfi, \n    personalizedRecommendations, \n    trackingFocus,\n    voiceMetrics,\n    isVoiceEnabled,\n    toggleVoiceAnalysis,\n    calibrateVoice,\n    state\n  } = useNeuroFog()\n\n  const profile = getPersonalizationProfile(userType)\n\n  return (\n    <div className={`min-h-screen bg-gradient-to-br ${profile.bgGradient}`}>\n      {/* User Type Header */}\n      <div className=\"p-6 border-b\"\n           style={{ backgroundColor: `${profile.primaryColor}15` }}>\n        <div className=\"flex items-center justify-between\">\n          <div className=\"flex items-center gap-3\">\n            <span className=\"text-3xl\">{profile.emoji}</span>\n            <div>\n              <h1 className=\"text-2xl font-bold\"\n                  style={{ color: profile.primaryColor }}>\n                NeuroFog for {profile.name}s\n              </h1>\n              <p className=\"text-gray-600\">\n                Personalized cognitive wellness tracking\n              </p>\n            </div>\n          </div>\n          \n          {/* Tracking Focus Pills */}\n          <div className=\"flex gap-2\">\n            {trackingFocus.map((focus, index) => (\n              <Badge \n                key={focus} \n                variant=\"secondary\"\n                className={`${\n                  index === 0 ? 'ring-2' : ''\n                }`}\n                style={{ \n                  borderColor: profile.accentColor,\n                  color: profile.accentColor\n                }}\n              >\n                {focus.charAt(0).toUpperCase() + focus.slice(1)} Priority\n              </Badge>\n            ))}\n          </div>\n        </div>\n      </div>\n\n      <div className=\"grid grid-cols-1 lg:grid-cols-3 gap-6 p-6\">\n        {/* Personalized CFI Widget */}\n        <Card className=\"p-6\">\n          <h3 className=\"font-semibold mb-4 flex items-center gap-2\">\n            <span className=\"text-xl\">{profile.emoji}</span>\n            {userType === 'developer' && 'Code Stress Index'}\n            {userType === 'student' && 'Study Load Index'}\n            {userType === 'creator' && 'Creative Flow Index'}\n          </h3>\n          \n          <div className=\"relative\">\n            <div className=\"text-4xl font-bold mb-2\"\n                 style={{ color: profile.primaryColor }}>\n              {personalizedCfi.toFixed(1)}\n            </div>\n            <Progress \n              value={personalizedCfi} \n              className=\"h-3 mb-4\"\n              style={{ \n                backgroundColor: `${profile.primaryColor}20`\n              }}\n            />\n            \n            <div className=\"text-sm text-gray-600\">\n              {personalizedCfi < 30 && `✨ Optimal ${userType} state`}\n              {personalizedCfi >= 30 && personalizedCfi < 60 && `⚡ Elevated ${userType} activity`}\n              {personalizedCfi >= 60 && personalizedCfi < 80 && `⚠️ High ${userType} stress`}\n              {personalizedCfi >= 80 && `🚨 Critical ${userType} overload`}\n            </div>\n          </div>\n        </Card>\n\n        {/* Voice Analysis Widget */}\n        <Card className=\"p-6\">\n          <div className=\"flex items-center justify-between mb-4\">\n            <h3 className=\"font-semibold flex items-center gap-2\">\n              🎤 Voice Analysis\n              {isVoiceEnabled && (\n                <Badge variant=\"outline\" className=\"text-green-600 border-green-600\">\n                  Live\n                </Badge>\n              )}\n            </h3>\n            <Button\n              onClick={toggleVoiceAnalysis}\n              variant={isVoiceEnabled ? \"destructive\" : \"default\"}\n              size=\"sm\"\n            >\n              {isVoiceEnabled ? 'Stop' : 'Start'}\n            </Button>\n          </div>\n          \n          {isVoiceEnabled && voiceMetrics ? (\n            <div className=\"space-y-3\">\n              <div className=\"flex justify-between items-center\">\n                <span className=\"text-sm text-gray-600\">Emotional State:</span>\n                <Badge variant=\"outline\">\n                  {voiceMetrics.emotionalTone === 'calm' && '😌 Calm'}\n                  {voiceMetrics.emotionalTone === 'neutral' && '😐 Neutral'}\n                  {voiceMetrics.emotionalTone === 'stressed' && '😰 Stressed'}\n                  {voiceMetrics.emotionalTone === 'excited' && '🤩 Excited'}\n                  {voiceMetrics.emotionalTone === 'tired' && '😴 Tired'}\n                </Badge>\n              </div>\n              \n              <div className=\"space-y-2\">\n                <div className=\"flex justify-between text-sm\">\n                  <span>Stress Level:</span>\n                  <span>{(voiceMetrics.stressIndicators * 100).toFixed(0)}%</span>\n                </div>\n                <Progress value={voiceMetrics.stressIndicators * 100} className=\"h-2\" />\n              </div>\n              \n              <div className=\"flex justify-between text-sm\">\n                <span>Speech Clarity:</span>\n                <span>{(voiceMetrics.clarity * 100).toFixed(0)}%</span>\n              </div>\n              \n              <Button onClick={calibrateVoice} variant=\"outline\" size=\"sm\" className=\"w-full\">\n                Calibrate Voice Baseline\n              </Button>\n            </div>\n          ) : (\n            <div className=\"text-center py-8 text-gray-500\">\n              {userType === 'developer' && '🗣️ Track stress during calls & debugging'}\n              {userType === 'student' && '📢 Monitor presentation confidence'}\n              {userType === 'creator' && '🎙️ Analyze creative expression patterns'}\n            </div>\n          )}\n        </Card>\n\n        {/* Personalized Recommendations */}\n        <Card className=\"p-6\">\n          <h3 className=\"font-semibold mb-4 flex items-center gap-2\">\n            💡 Personalized Tips\n          </h3>\n          \n          <div className=\"space-y-3\">\n            {personalizedRecommendations.length > 0 ? (\n              personalizedRecommendations.map((rec, index) => (\n                <div key={index} className=\"p-3 rounded-lg\"\n                     style={{ backgroundColor: `${profile.primaryColor}10` }}>\n                  <div className=\"text-sm\"\n                       style={{ color: profile.accentColor }}>\n                    {rec}\n                  </div>\n                </div>\n              ))\n            ) : (\n              <div className=\"text-center py-6 text-gray-500\">\n                <div className=\"text-2xl mb-2\">{profile.emoji}</div>\n                <div className=\"text-sm\">\n                  {userType === 'developer' && 'Keep coding! We\\'ll notify you if stress patterns emerge.'}\n                  {userType === 'student' && 'Great focus! We\\'re monitoring your study patterns.'}\n                  {userType === 'creator' && 'Creative flow detected! We\\'ll help maintain your momentum.'}\n                </div>\n              </div>\n            )}\n          </div>\n        </Card>\n      </div>\n\n      {/* User-Specific Features */}\n      <div className=\"p-6\">\n        <h2 className=\"text-xl font-semibold mb-4\">\n          {userType === 'developer' && '🛠️ Developer Tools'}\n          {userType === 'student' && '📖 Study Features'}\n          {userType === 'creator' && '🎨 Creative Tools'}\n        </h2>\n        \n        <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4\">\n          {/* Developer-specific features */}\n          {userType === 'developer' && (\n            <>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🐛 Debug Session Tracking</h4>\n                <p className=\"text-sm text-gray-600\">Monitor stress during debugging</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">📝 Commit Pattern Analysis</h4>\n                <p className=\"text-sm text-gray-600\">Track coding productivity cycles</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🧠 Focus Deep Dive</h4>\n                <p className=\"text-sm text-gray-600\">Extended concentration analytics</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">☕ Break Optimizer</h4>\n                <p className=\"text-sm text-gray-600\">Personalized coding break timing</p>\n              </Card>\n            </>\n          )}\n          \n          {/* Student-specific features */}\n          {userType === 'student' && (\n            <>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">⏱️ Study Timer</h4>\n                <p className=\"text-sm text-gray-600\">Pomodoro with cognitive awareness</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">📊 Retention Tracking</h4>\n                <p className=\"text-sm text-gray-600\">Learning efficiency patterns</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🎯 Focus Analysis</h4>\n                <p className=\"text-sm text-gray-600\">Optimal study period detection</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">📝 Exam Prep Mode</h4>\n                <p className=\"text-sm text-gray-600\">Stress management during tests</p>\n              </Card>\n            </>\n          )}\n          \n          {/* Creator-specific features */}\n          {userType === 'creator' && (\n            <>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🎨 Creative Flow State</h4>\n                <p className=\"text-sm text-gray-600\">Track and optimize flow periods</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">💡 Inspiration Capture</h4>\n                <p className=\"text-sm text-gray-600\">Voice memos during peak creativity</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🌈 Mood-Color Correlation</h4>\n                <p className=\"text-sm text-gray-600\">Creative state visualization</p>\n              </Card>\n              <Card className=\"p-4\">\n                <h4 className=\"font-medium mb-2\">🔄 Block Breaker</h4>\n                <p className=\"text-sm text-gray-600\">AI-powered creative block detection</p>\n              </Card>\n            </>\n          )}\n        </div>\n      </div>\n    </div>\n  )\n}